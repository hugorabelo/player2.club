<?php

class UsuariogruposTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('usuariogrupos')->truncate();

		$usuariogrupos = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('usuariogrupos')->insert($usuariogrupos);
	}

}
