@extends('layouts.scaffold')

@section('main')

<h1>{{ trans('messages.{{models}}_list') }}</h1>

<p>{{ link_to_route('{{models}}.create', trans('messages.{{model}}_add'), null, array('class' => 'btn btn-lg btn-success')) }}</p>

@if (${{models}}->count())
	<table class="table table-striped">
		<thead>
			<tr>
				{{headings}}
				<th>&nbsp;</th>
			</tr>
		</thead>

		<tbody>
			@foreach (${{models}} as ${{model}})
				<tr>
					{{fields}}
				</tr>
			@endforeach
		</tbody>
	</table>
@else
	{{ trans('messages.{{model}}_empty') }}
@endif

@stop
